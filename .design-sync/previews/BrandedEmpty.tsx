import * as React from 'react';
import { BrandedEmpty } from '@spicychicken/react';
export const Shortlist = () => <BrandedEmpty title="Nothing on the shortlist yet." description="The next run will bring the next candidates." />;
