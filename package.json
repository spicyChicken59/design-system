{
  "name": "spicychicken-design-system",
  "version": "2.3.0",
  "private": true,
  "license": "MIT",
  "description": "SpicyChicken design system: sc.css (tokens + base + components), generated tokens.json and style guide, optional React wrappers.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "npm --prefix react ci",
    "build": "node build/gen-tokens.mjs && node build/assemble.mjs && npm --prefix react run build",
    "check": "node build/check.mjs",
    "build:react": "npm --prefix react run build",
    "lint:consumer": "node build/consumer-lint.mjs"
  }
}
